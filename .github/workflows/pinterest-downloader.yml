name: Pinterest Image Downloader

on:
  workflow_dispatch:
    inputs:
      search_query:
        description: 'Search term (e.g., kagamine len)'
        required: true
        default: 'kagamine len'
        type: string
      max_results:
        description: 'Number of images'
        required: true
        default: 50
        type: number

jobs:
  download:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install requests

      - name: 🚀 Download Pinterest Images
        run: |
          cat > downloader.py << 'EOF'
          import requests
          import json
          import time
          import random
          import os
          import re
          from urllib.parse import quote
          
          SEARCH = "${{ github.event.inputs.search_query }}"
          MAX_RESULTS = int("${{ github.event.inputs.max_results }}")
          DOWNLOAD_DIR = "downloads"
          
          os.makedirs(DOWNLOAD_DIR, exist_ok=True)
          
          print("="*50)
          print(f"Searching: {SEARCH}")
          print(f"Target: {MAX_RESULTS} images")
          print("="*50)
          
          session = requests.Session()
          session.headers.update({
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
              'Accept': 'application/json, text/javascript, */*; q=0.01',
              'Accept-Language': 'en-US,en;q=0.9',
              'Referer': 'https://www.pinterest.com/',
              'Origin': 'https://www.pinterest.com',
          })
          
          # Get initial cookie
          print("Getting session...")
          session.get('https://www.pinterest.com/', timeout=30)
          
          images = []
          bookmark = ""
          page = 0
          
          while len(images) < MAX_RESULTS and page < 20:
              page += 1
              print(f"Page {page}...")
              
              data = {
                  "options": {
                      "query": SEARCH,
                      "bookmarks": [bookmark] if bookmark else [],
                      "page_size": min(50, MAX_RESULTS - len(images))
                  }
              }
              
              params = {
                  "source_url": f"/search/pins/?q={quote(SEARCH)}",
                  "data": json.dumps(data)
              }
              
              time.sleep(random.uniform(1, 2))
              
              try:
                  resp = session.get(
                      "https://www.pinterest.com/resource/BaseSearchResource/get/",
                      params=params,
                      timeout=30
                  )
                  
                  if resp.status_code == 429:
                      print("Rate limit, waiting...")
                      time.sleep(30)
                      continue
                  
                  if resp.status_code != 200:
                      print(f"HTTP {resp.status_code}")
                      break
                  
                  result = resp.json()
                  pins = result.get("resource_response", {}).get("data", {}).get("results", [])
                  
                  if not pins:
                      print("No more results")
                      break
                  
                  for pin in pins:
                      img_data = pin.get("images", {})
                      img_url = None
                      
                      # Get best quality
                      for quality in ["originals", "736x", "564x"]:
                          if quality in img_data:
                              img_url = img_data[quality].get("url")
                              if img_url:
                                  break
                      
                      if img_url:
                          # Clean title for filename
                          raw_title = pin.get("title", f"img_{len(images)}")
                          clean_title = re.sub(r'[^\w\s-]', '', raw_title)[:30]
                          clean_title = clean_title.replace(' ', '_')
                          
                          images.append({
                              "url": img_url,
                              "id": pin.get("id", str(len(images))),
                              "title": clean_title
                          })
                          print(f"  Found: {len(images)}/{MAX_RESULTS}")
                          
                          if len(images) >= MAX_RESULTS:
                              break
                  
                  bookmark = result.get("resource_response", {}).get("bookmark", "")
                  if not bookmark:
                      break
                      
              except Exception as e:
                  print(f"Error: {e}")
                  break
          
          print(f"\nFound {len(images)} images. Downloading...")
          
          downloaded = 0
          for idx, img in enumerate(images, 1):
              try:
                  # Build filename safely
                  safe_title = img['title'].replace('/', '_').replace('\\', '_')
                  filename = f"{DOWNLOAD_DIR}/{idx:04d}_{safe_title}.jpg"
                  
                  resp = session.get(img['url'], timeout=30)
                  if resp.status_code == 200 and len(resp.content) > 5000:
                      with open(filename, 'wb') as f:
                          f.write(resp.content)
                      downloaded += 1
                      size_kb = len(resp.content) // 1024
                      print(f"✅ [{idx}/{len(images)}] Downloaded ({size_kb}KB)")
                  else:
                      print(f"❌ [{idx}/{len(images)}] Failed (HTTP {resp.status_code})")
                  
                  time.sleep(0.5)
                  
              except Exception as e:
                  print(f"❌ [{idx}] Error: {str(e)[:50]}")
          
          print(f"\n{'='*50}")
          print(f"Complete! Downloaded {downloaded}/{len(images)} images")
          print(f"Saved in: {DOWNLOAD_DIR}/")
          print(f"{'='*50}")
          
          # Save metadata
          with open(f"{DOWNLOAD_DIR}/metadata.json", "w") as f:
              json.dump({
                  "query": SEARCH,
                  "total_found": len(images),
                  "downloaded": downloaded,
                  "timestamp": time.strftime("%Y-%m-%d %H:%M:%S")
              }, f, indent=2)
          EOF
          
          python downloader.py

      - name: 📦 Create ZIP
        run: |
          if [ -d "downloads" ] && [ "$(ls -A downloads | grep -E '\.jpg$' | head -1)" ]; then
            ZIP_NAME="pinterest_$(date +'%Y%m%d_%H%M%S').zip"
            zip -r "$ZIP_NAME" downloads/
            echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV
            IMG_COUNT=$(ls downloads/*.jpg 2>/dev/null | wc -l)
            echo "✅ Created ZIP with $IMG_COUNT images"
            echo "IMAGE_COUNT=$IMG_COUNT" >> $GITHUB_ENV
          else
            ZIP_NAME="empty_$(date +'%Y%m%d_%H%M%S').zip"
            echo "No images found for '${{ github.event.inputs.search_query }}'" > result.txt
            zip "$ZIP_NAME" result.txt
            echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV
            echo "IMAGE_COUNT=0" >> $GITHUB_ENV
            echo "⚠️ No images were found"
          fi

      - name: 💾 Push to repository
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add "${{ env.ZIP_NAME }}" || true
          git add downloads/ || true
          git diff --staged --quiet || git commit -m "📸 Pinterest: ${{ github.event.inputs.search_query }} (${{ env.IMAGE_COUNT }} images)"
          git push origin HEAD:${{ github.ref_name }} || true
          echo "✅ Pushed to repository"

      - name: 📊 Summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 🎉 Pinterest Download Results
          
          | آیتم | مقدار |
          |------|-------|
          | 🔍 جستجو | ${{ github.event.inputs.search_query }} |
          | 🖼️ تصاویر دانلود شده | ${{ env.IMAGE_COUNT }} |
          | 📦 فایل ZIP | [${{ env.ZIP_NAME }}](${{ github.server_url }}/${{ github.repository }}/blob/${{ github.ref_name }}/${{ env.ZIP_NAME }}) |
          
          **📥 نحوه دانلود:** روی لینک بالا کلیک کنید → راست کلیک → Save Link As
          
          ---
          *این فایل به صورت خودکار توسط GitHub Actions تولید شده است*
          EOF
